import { ServiceSelector } from '@/components/dashboard/ServiceSelector';

export const metadata = { title: 'Mis servicios — IAM Portal' };

export default function DashboardPage() {
  return <ServiceSelector />;
}
